spring:
  datasource:
    username: root
    password: root
    url: jdbc:db://localhost:3306/examples
  application:
    name: simple-spring-config
# disable consul
  cloud:
    consul:
      enabled: false
server:
  port: 5120